# AHA 2026 pipeline — pinned dependencies
# Python 3.10+ required (uses `str | None` PEP-604 syntax)

# LLM SDKs
anthropic>=0.39.0,<1.0.0
openai>=1.50.0,<2.0.0
google-genai>=0.3.0,<1.0.0

# Verilog parsing & AST manipulation
pyverilog>=1.3.0,<2.0.0

# Utilities
jsonschema>=4.20.0,<5.0.0     # for prompt-output schema validation
PyYAML>=6.0,<7.0              # for models.yaml manifest

# Testing & dev (install with: pip install -r requirements-dev.txt)
# pytest>=8.0.0
# pytest-cov>=5.0.0
# ruff>=0.6.0
